package library.admin.controller;

import library.admin.domain.MemberVO;
import library.admin.service.MemberService;
import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;

import static org.junit.jupiter.api.Assertions.*;
@SpringBootTest
class MemberApiControllerTest {

/*
    @Autowired BCryptPasswordEncoder encoder;
    @Autowired
    MemberService memberService;
*/
    @Test
    public void testMethodNameHere() throws Exception {
        // given
/*        String admin = encoder.encode("admin");
        MemberVO member = MemberVO.builder().name("admin").password(admin).authority("ADMIN").build();
        // when
        memberService.join(member);*/



        // then
    }

}